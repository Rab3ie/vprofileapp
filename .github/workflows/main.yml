name: Build and Push to Harbor

on:
  push:
    branches:
      - local

jobs:
  build-and-push:
    runs-on: self-hosted  # Use your self-hosted runner to reach the local Harbor IP

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker

      - name: Log in to Harbor Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ vars.HARBOR_HOST }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Debug Harbor configuration
        run: |
            echo "HARBOR_HOST=${{ vars.HARBOR_HOST }}"
            echo "HARBOR_PROJECT=${{ vars.HARBOR_PROJECT }}"
            echo "IMAGE=${{ vars.HARBOR_HOST }}/${{ vars.HARBOR_PROJECT }}/my-app:latest"
      ### app ########
      - name: Build and Push app image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Docker-files/app/Dockerfile
          push: true
          tags: |
            ${{ vars.HARBOR_HOST }}/${{ vars.HARBOR_PROJECT }}/my-app:latest
            ${{ vars.HARBOR_HOST }}/${{ vars.HARBOR_PROJECT }}/my-app:${{ github.sha }}
      ### db ########
      - name: Build and Push db Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Docker-files/db/Dockerfile
          push: true
          tags: |
            ${{ vars.HARBOR_HOST }}/${{ vars.HARBOR_PROJECT }}/my-db:latest
            ${{ vars.HARBOR_HOST }}/${{ vars.HARBOR_PROJECT }}/my-db:${{ github.sha }}
      ### web ########
      - name: Build and Push web Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Docker-files/web/Dockerfile
          push: true
          tags: |
            ${{ vars.HARBOR_HOST }}/${{ vars.HARBOR_PROJECT }}/my-web:latest
            ${{ vars.HARBOR_HOST }}/${{ vars.HARBOR_PROJECT }}/my-web:${{ github.sha }}
